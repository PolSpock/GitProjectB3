<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.garrycity.pol.gitprojectb3">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:screenOrientation="portrait">
        <activity android:name=".activities.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SearchRepositoryActivity" />
        <activity android:name=".activities.SearchProfileActivity" />
        <activity android:name=".activities.SearchGistActivity" />
        <activity android:name=".activities.RepositoryViewActivity" />
        <activity android:name=".activities.ProfileViewActivity" />
        <activity android:name=".activities.ProfileViewReposActivity" />
        <activity android:name=".activities.ProfileViewOrgsActivity" />
        <activity android:name=".activities.GistViewActivity" />
        <activity android:name=".activities.CreateGistActivity" />
    </application>

</manifest>